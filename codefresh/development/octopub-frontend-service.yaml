apiVersion: v1
kind: Service
metadata:
  name: octopub-frontend-cluster-ip
  namespace: development
spec:
  type: ClusterIP
  selector:
    component: web
  ports:
    - port: 8080
      targetPort: 8080
      name: http-port